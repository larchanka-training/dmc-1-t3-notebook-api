# Copy this file to .env.test and fill in your local values.
# .env.test is gitignored; this example file is safe to commit.
#
# Credentials must match the postgres service in docker-compose.yaml.
TEST_DATABASE_URL=postgresql+psycopg://<user>:<password>@localhost:5432/notebook_test
ENVIRONMENT=development
LOG_LEVEL=WARNING
